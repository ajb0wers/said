[
	{kernel,
		[{logger_level, all},
		{logger,
			[{handler, default, logger_std_h,
				#{level => info,
          formatter => {logger_formatter, #{single_line => false}}}
      }
			%% ,{handler, file, logger_std_h,
			%% 	#{level => debug,
			%% 		config => #{file => "log/said.log"},
			%% 		formatter => {logger_formatter, #{single_line => true}}}
      %% }
			]
    }]},
  {said, []}
].
